apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
    name: postgres
spec:
    instances: 3
    storage:
        pvcTemplate:
            accessModes:
                - ReadWriteOnce
            storageClassName: local-storage
            resources:
                requests:
                    storage: 30Gi
            selector:
                matchLabels:
                    app: postgres-data
    bootstrap:
        initdb:
            database: app
            owner: app
