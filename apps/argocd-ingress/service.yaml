apiVersion: v1
kind: Service
metadata:
  name: argo-service
  namespace: argocd
spec:
  selector:
    app: argocd-server
  ports:
    - name: http
      port: 8081
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
