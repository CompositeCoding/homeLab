apiVersion: v1
kind: Service
metadata:
  name: argo-service
  namespace: argocd
spec:
  selector:
    app: argocd-server
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
